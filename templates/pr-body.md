<!--
  Use this only when the repository has no PR template. If it has one, fill in that template.
  Write in ASD-STE100 Simplified Technical English: short sentences, one statement each,
  active voice, no rhetorical phrases. Rules: references/pr-packaging.md, section 5.
  Add nothing below unless the user asks for it (methodology, verification table,
  reproduction commands, harness link, companion PRs).
-->

## What

<One or two sentences: what the change does, in which file.>

## Why

<One or two sentences: why the old code was slow and why the new code is faster.>

## Result

<Workload> takes <N>% less time (<M>x faster) on <runtime and version>.

<Tests that ran.> The behaviour is unchanged.

<Only if there is one: an observable difference, or an invariant the change introduces. One sentence each.>
